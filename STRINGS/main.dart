void main() {
  // strings
  // we can use use string for any word/senetence /statemnet like in the ""in this .
  var name = "afshan";
  var fathername = "qasim";
  final age = 19;
  // print("my name is $name and father name is $fathername and age is $age");
  // if we add '''here ''' like this strings then it create the gap with the way we do
  // example
  // print(''' hello
  // my name is $name
  // my fathername is $fathername
  // and
  // age is $age''');
  // methods for strings
  // length /index
  // print(name.length);
  // print(name[2]);
  // uppercase/lowercase
  // print(name.toLowerCase());
  // print(name.toUpperCase());
  // split (this method spilit the whole word by ('')and change it by (*))
  // print(name.split('*'));
  // replace (this will replace your string with some methods)
  // print(name.replaceAll(name, 'replace'));
  // print(name.replaceFirst('f', 'b'));
  // contain (this checks that word exist or not then give you true r false )
  print(name.contains('j'));
  print(name.contains('h'));
}
